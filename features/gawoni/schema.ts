export interface GawoniParams {
  ga_pool: { label: string; rate: number }[];
  isRandomOrder: boolean;
  isRaw: boolean;
  ni_pool: { label: string; rate: number }[];
  wo_pool: { label: string; rate: number }[];
}
